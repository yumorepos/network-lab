# Data: raw downloads and derived stores are rebuilt by `make data`; only
# small hand-compiled reference files under data/reference/ are committed.
data/raw/
data/parquet/
data/*.duckdb
data/*.duckdb.wal

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# OS / editor
.DS_Store
*.swp

# Local scratch and archived design history (kept locally, not published)
scratch/
archive/
